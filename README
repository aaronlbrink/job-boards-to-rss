# Job Boards to RSS
This python script fetches jobs from company job boards, filters them to your search criteria, and generates an RSS feed of your job matches.

## Installation

```
python -m venv env
source venv/bin/activate
pip install -r requirements.txt
pip install -e .
python -m src.main
```
